<div class="field-unit__label">
  <%= f.label field.attribute %>
</div>
<div class="field-unit__field">
  <% field.resource_name = f.object_name %>
  <% field.resource_instance = f.object %>
  <% sel_options = field.selectable_options %>
  <% if sel_options.length < 4 %>
    <%= f.collection_radio_buttons(
      field.attribute,
      sel_options,
      :last,
      :first
    ) do |b| %>
      <%= b.radio_button %>&nbsp;<%= b.text %>&nbsp;&nbsp;
    <% end %>
  <% else %>
    <%= f.select(
      field.attribute,
      options_from_collection_for_select(
        sel_options,
        :last,
        :first,
        field.data.presence
      )
    ) %>
  <% end %>
</div>
